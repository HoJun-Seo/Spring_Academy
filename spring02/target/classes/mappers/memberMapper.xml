<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mysqldbMember">
	<select id="list" resultType="com.example.dto.MemberDTO">
		select *
			from member
		order by
			name
	</select>
</mapper>